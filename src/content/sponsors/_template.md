---
name: "Sponsor Name"
date: "2026-01-01"
current: false
description: "One-line summary shown in the timeline list."
url: "https://sponsor-website.com"
logo: "https://link-to-sponsor-logo.png"
linkedin: "https://linkedin.com/in/sponsor"
x: "https://x.com/sponsor"
---

Write a longer blurb about the sponsor here, such as how they are supporting your work, what their product does, or any other relevant details. This is only shown when the sponsor is featured (`current: true`).

*(Note: If `logo` is empty or removed, the website will generate a fallback placeholder based on the sponsor's name. Set `current: true` on at most one sponsor to feature them prominently at the top of the page.)*

*(Note: Each sponsorship gets its own page at `/sponsors/<filename>/`. If the same sponsor backs you again for a different event, don't reuse the existing file — create a new one with a distinct filename, e.g. `sponsor-name-event-2027.md`. Reuse the same `name` so it reads consistently in the timeline, but each file is a separate sponsorship with its own date and URL.)*


